machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t centos/6-deploy:0.1.0 -f docker/Dockerfile .

test:
  override:
    - docker run centos/6-deploy:0.1.0 /bin/bash -c 'cd /opt/serverspec; rake spec;'

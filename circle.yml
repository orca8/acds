machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t centos/6-deploy:0.1.0 ./docker/

test:
  override:
    - docker run centos/6-deploy:0.1.0 /bin/bash
    - cd serverspec && rake spec
